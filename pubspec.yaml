name: linguago
description: "Offline voice translation powered by on-device Gemma 4. No servers, no accounts, no network."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # State management (Riverpod is the only state-management solution used).
  flutter_riverpod: ^2.6.1

  # Immutable models + JSON serialization for Language / TranslationResult / AppSettings.
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  flutter_gemma: ^1.6.5
  flutter_gemma_litertlm: ^1.5.3
  connectivity_plus: ^7.3.1
  record: ^7.1.1
  path_provider: ^2.1.6
  background_downloader: ^9.5.9
  flutter_tts: ^4.2.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  build_runner: 2.5.4
  freezed: ^3.0.0
  json_serializable: ^6.8.0
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/Images/

  # Bundled rather than fetched at runtime. `google_fonts` downloads from
  # fonts.gstatic.com on first launch and silently falls back to a system font
  # when offline — which contradicts the whole point of this app (PRD §21: no
  # silent network fetches). Shipping the TTFs costs ~500 KB and makes the
  # typography work with the radio off.
  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/PlusJakartaSans-Regular.ttf
          weight: 400
        - asset: assets/fonts/PlusJakartaSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/PlusJakartaSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/PlusJakartaSans-Bold.ttf
          weight: 700
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# win32 6.4.0 uses Dart 3.12 "dot shorthand" syntax (`Data1 = .parse(...)`),
# which the analyzer bundled with freezed 2.x cannot parse — it throws
# "Missing implementation of visitDotShorthandInvocation" and codegen dies.
# Upgrading freezed is blocked: freezed 4 needs Dart >=3.13, and the Flutter
# SDK pins meta 1.18.0 which caps the analyzer below what build_runner 2.15+
# wants. win32 is a Windows-only transitive dependency of path_provider /
# connectivity_plus / device_info_plus, and this app targets Android and iOS,
# so holding it back costs us nothing. Remove once the toolchain moves on.
dependency_overrides:
  win32: ^5.5.4
  win32_registry: ^1.1.5

# App icon generation: `dart run flutter_launcher_icons`
#
# Sources live in assets/icon/ and are build-time inputs only — they are
# deliberately not in the `assets:` list above, since nothing loads them at
# runtime.
flutter_launcher_icons:
  image_path: "assets/icon/app_icon.png"
  android: true
  ios: true
  min_sdk_android: 24

  # iOS rejects icons with an alpha channel; flatten onto the logo's navy.
  remove_alpha_ios: true
  background_color_ios: "#0B1C3D"

  # Android adaptive icon: the launcher masks the foreground to whatever shape
  # the device uses, guaranteeing only the central ~66%. app_icon_foreground
  # is already sized to 60% for that safe zone, so no further inset.
  adaptive_icon_background: "#0B1C3D"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
  adaptive_icon_foreground_inset: 0
